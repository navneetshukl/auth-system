version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailhog
    ports:
      - "1025:1025"  # SMTP port — use this in your app to send mail
      - "8025:8025"  # Web UI — open http://localhost:8025 to view emails
    restart: unless-stopped
